---
let selectPro = true;

/* function toggleSelect(): void {
  selectPro = !selectPro;
}

document.getElementById("iphone")?.addEventListener("click", function () {
  toggleSelect();
}); */
---

<section class="flex flex-row gap-3">
  <button
    id="iphone"
    class:list={[
      "px-5 py-3 font-medium text-xl text-center  rounded-lg cursor-pointer ",
      {
        "text-blue-500 hover:text-white border-4 border-blue-500 hover:bg-blue-500 focus:ring-4 focus:outline-none hover:shadow-lg focus:ring-blue-300":
          selectPro,
        "inline-flex items-center justify-center text-black": !selectPro,
      },
    ]}
  >
    iPhone 15 Pro Max
  </button>

  <button
    id="iphone"
    class:list={[
      "px-5 py-3 font-medium text-xl text-center  rounded-lg cursor-pointer ",
      {
        "text-blue-500 hover:text-white border-4 border-blue-500 hover:bg-blue-500 focus:ring-4 focus:outline-none hover:shadow-lg focus:ring-blue-300":
          !selectPro,
        "inline-flex items-center justify-center text-black": selectPro,
      },
    ]}
    >iPhone 15
  </button>
</section>
