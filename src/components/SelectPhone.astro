---
let selectPro = true;

/* function toggleSelect(): void {
  selectPro = !selectPro;
}

document.getElementById("iphone")?.addEventListener("click", function () {
  toggleSelect();
}); */
---

<section class="flex flex-row gap-3">
  <button
    id="iphone"
    class:list={[
      "px-5 py-3 font-semibold text-xl text-center  rounded-lg",
      {
        "text-blue-500 border-4 border-blue-500 cursor-default ": selectPro,
        "inline-flex items-center justify-center text-black  hover:text-white cursor-pointer  hover:bg-blue-500":
          !selectPro,
      },
    ]}
  >
    iPhone 15 Pro Max
  </button>

  <button
    id="iphone"
    class:list={[
      "px-5 py-3 font-semibold text-xl text-center rounded-lg",
      {
        "text-blue-500 hover:text-white border-4 border-blue-500 cursor-default":
          !selectPro,
        "inline-flex items-center justify-center text-black  hover:text-white  cursor-pointer  hover:bg-blue-500":
          selectPro,
      },
    ]}
    >iPhone 15
  </button>
</section>
